  <!-- NAVBAR LAYOUT -->
  <header>
    <div class="u-full-width hero navbar" id="concept">
      <div class="row">
        <div class="five columns"><%=image_tag "polygons.png", class:"polygon-navbar"%></div>
        <div class="six columns"><a href="#"><%= image_tag "LOGO.svg", class:"logo"%></a></div>
        <div class="one column dropdown">
          <a href="#"><%= image_tag "hamburger.svg", class:"hamburger"%></a>
            <div class="dropdown-content">
              <ul><li><%if user_signed_in?%>
              <%=link_to "Accueil", user_path(current_user.id), class:"link"%>
              <%else%>
              <%=link_to "Accueil", root_path, class:"link"%>
              <%end%></li>
            <li><%= link_to "Contact", new_contact_path, class:"link"%></li>
            <% if user_signed_in? %>
            <%=link_to "Deconnexion", destroy_user_session_path, :method => :delete, class:"link"%>
            <% else%>
            <%=link_to "Connexion", new_user_session_path, class:"link"%>
            <%end%>
              </ul>
          </div>
        </div>
        </div>
        </div>
  </header>

