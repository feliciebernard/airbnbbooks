<%= form_with(model: private_address, local: true) do |form| %>
  <% if private_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(private_address.errors.count, "error") %> prohibited this private_address from being saved:</h2>

      <ul>
      <% private_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :street_name %>
    <%= form.text_field :street_name %>
  </div>

  <div class="field">
    <%= form.label :other_information %>
    <%= form.text_field :other_information %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
